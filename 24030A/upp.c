#include<stdio.h>
#include<ctype.h>
#include<string.h>

void touppercase(char str[])
{
    int i;
    for(i=0;str[i]!= '\0';i++)
    {
        if(str[i]>= 'A' && str[i]<= 'Z')
        {
            
            
        }
    }
}

int main() 
{en(str);
    if(str[len -1] == '\n')
    {
        str[len-1] = '\0';
    }
    touppercase(str);
    printf("upper case string is: %s\n", str);
    return 0;
}